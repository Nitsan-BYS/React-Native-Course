import React from 'react';
import ReactDOM from 'react-dom/client';
import TiltSquare from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TiltSquare />
  </React.StrictMode>
);

